<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".GameActivity">

    <!-- Dealer's Hand -->
    <LinearLayout
        android:id="@+id/dealerHandLayout"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/playerHandLayout">

        <!-- Face-up card for the dealer -->
        <ImageView
            android:id="@+id/dealerCard1"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:src="@drawable/card_back_red"
            android:scaleType="centerInside"
            android:layout_marginEnd="8dp"/>

        <!-- Face-down card for the dealer -->
        <ImageView
            android:id="@+id/dealerCard2"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:src="@drawable/card_back_red"
            android:scaleType="centerInside"/>

        <!-- Add more ImageViews for additional dealer cards if needed -->
    </LinearLayout>

    <!-- Player's Hand -->
    <LinearLayout
        android:id="@+id/playerHandLayout"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@+id/dealerHandLayout"
        app:layout_constraintBottom_toTopOf="@+id/buttonsLayout">

        <!-- Face-up card for the player -->
        <ImageView
            android:id="@+id/playerCard1"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:src="@drawable/card_back_red"
            android:scaleType="centerInside"
            android:layout_marginEnd="8dp"/>

        <!-- Face-up card for the player -->
        <ImageView
            android:id="@+id/playerCard2"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:src="@drawable/card_back_red"
            android:scaleType="centerInside"/>

        <!-- Add more ImageViews for additional player cards if needed -->
    </LinearLayout>

    <!-- Buttons for Hit and Stand -->
    <LinearLayout
        android:id="@+id/buttonsLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <Button
            android:id="@+id/btnHit"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Hit"/>

        <Button
            android:id="@+id/btnStand"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Stand"/>

        <Button
            android:id="@+id/btnDoubleDown"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="2x"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>